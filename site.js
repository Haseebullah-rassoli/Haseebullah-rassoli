document.addEventListener("DOMContentLoaded",()=>{const toggle=document.querySelector(".mobile-toggle"),nav=document.querySelector(".nav-links");if(toggle&&nav){toggle.addEventListener("click",()=>{const show=nav.classList.toggle("is-open");toggle.setAttribute("aria-expanded",String(show));toggle.textContent=show?"✕ Close":"☰ Menu"});}document.querySelectorAll(".nav-drop").forEach(d=>{d.addEventListener("toggle",()=>{if(d.open)document.querySelectorAll(".nav-drop").forEach(other=>{if(other!==d)other.open=false;});});});document.addEventListener("click",event=>{if(!event.target.closest(".nav-drop"))document.querySelectorAll(".nav-drop").forEach(d=>d.open=false);});const s=document.querySelector("#opportunity-search"),cat=document.querySelector("#opportunity-filter");function apply(){document.querySelectorAll("[data-search]").forEach(card=>{card.hidden=!!((s&&!card.dataset.search.includes(s.value.toLowerCase()))||(cat&&cat.value&&cat.value!==card.dataset.category));});}if(s)s.addEventListener("input",apply);if(cat)cat.addEventListener("change",apply);
const reviewForm=document.getElementById("review-form");if(reviewForm){const upload=document.getElementById("report-attachment"),message=document.getElementById("attachment-error"),maxSize=5*1024*1024;const checkUpload=()=>{const file=upload.files&&upload.files[0];if(!file){upload.setCustomValidity("");message.hidden=true;message.textContent="";return true;}const validType=["image/jpeg","image/png"].includes(file.type)||(!file.type&&/\.(jpg|jpeg|png)$/i.test(file.name));let error="";if(!validType)error="Please choose a PNG or JPEG image. Other file types are not supported.";else if(file.size>maxSize)error="This screenshot is larger than 5 MB. Please crop or compress it before submitting.";else if(file.size===0)error="The screenshot appears to be empty. Please choose another image.";upload.setCustomValidity(error);message.textContent=error;message.hidden=!error;return !error;};upload.addEventListener("change",checkUpload);reviewForm.addEventListener("submit",event=>{if(!checkUpload()){event.preventDefault();upload.focus();upload.reportValidity();return;}const button=reviewForm.querySelector('button[type="submit"]');if(button){button.disabled=true;button.textContent="Sending report…";} });}
});